module Tessarium.Check.Table

/// Deliberately empty: nothing imports this module -- it exists to be
/// CHECKED, and its assertions are its content. The interface is here
/// because `friend` is only permitted in modules that have one.
