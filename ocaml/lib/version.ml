(* The core's own version: fstar/ and ocaml/, bumped when either changes.

   Not what a release is named. That is (version) in dune-project, which moves
   when this or the dashboard does. The two need no parity, so this number
   says only how far the core has come. *)

let core = "0.2.3"
